#include <stdio.h>

/**
  * main - check the code
  *
  * Return: Always 0 (Success).
  */
int main(void)
{
	int r;
		print_last_digit(98);
		print_last_digit(0);
		r = print_last_digit;
		_putchar('0' + r);
		return (0);
}

