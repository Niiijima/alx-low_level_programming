#include <stdio.h>

/**
  * main - check the code
  * Return: Always 0 (Success)
  */
int main(void)
{
	int num = -5;

	int abs_value = _abs(num);

	printf("The absolute value of %d is %d\n", num, abs_value);

	return 0;
}
